# M5：多轮结构化问诊原文保留

日期：2026-09-24 UTC。开发基线：`e5d53884789115ffdb3a47d9303d88c4570bf915`（M3 main）。
分支：`feat/consult-structured-history-m5-20260924`。
交付边界：九文件、草稿 PR、新提交 CI；不自动合并或部署。
PR #41 / M4 仍是独立草稿，本分支不包含其未合并修改。

## 目标与实现

此前结构化答案只进入当轮 AI 上下文，未随会话 answers 保存；下一轮及刷新后丢失，首次保存又依赖前端最后一份内存快照。本次修复既有数据保留链路，不新增医学问题或诊疗解释。

- 复用 ConsultSession.answers 现有 JSON 列，在普通 question/answer 项内加入 JSON 字符串 `structured_intake_snapshot`，保持既有字符串字典响应兼容。保留模板键/版本、分组/问题键及标签、答案原文和来源轮次；不改模型或数据库结构。
- 类型、未知字段、数量和长度在 AI 调用/写入前校验。最多 60 组、每组 200 项、合计 600 项；单项原文 100,000 字符，元数据 1,000 字符，单快照序列化 250,000 字符，会话快照累计 1,000,000 字符。超限返回 422；损坏的既存快照阻止继续保存，不静默丢弃。
- 后续上下文读取所有已保留快照；保存前预览、首次保存、明确选择 consult_sync 的更新包含这些原文。history_only 仍只追加医生补记，不偷偷同步新问诊内容。
- 原文不 trim、不把空白转换为“无异常”。轮次/版本和原文结束标记位于答案之外；否定词、换行、Unicode、字面 HTML 保留并作为文本显示。完整来源块重复附加被抑制，不合并不同轮次的相同答案。第三轮更正保留前两轮。
- GET/成功响应显示已提交记录，待提交输入独立；请求绑定当前会话和账号，迟到响应不应用到另一会话/账号。提交锁抑制双击；结果不确定时保留输入并只允许 GET 核对，不自动再次 POST。
- 可选 `expected_answers_token` 绑定原答案序列；旧轮次重放在 AI 调用前返回 409。PostgreSQL 行锁与条件更新防止覆盖竞争写入。旧客户端不传令牌仍可用；不声称所有旧客户端或跨客户端操作具有幂等性。
- 旧会话没有快照时照常读写，不反推或回填已经丢失的答案。兼容旧客户端保存时再次携带最新快照，避免同一回执重复附加。
- 文书复用已有核对/导出路径与原模板，导出最终已保存的病例原文；仍是待医生核对、未签署草稿。

## 本次九文件

1. backend/main.py
2. frontend/src/App.jsx
3. frontend/tests/consult-first-save.test.jsx
4. tests/test_consult_first_save.py
5. tests/test_consult_update_preview.py
6. tests/acceptance/draft_checks.cjs
7. tests/acceptance/visit_document_checks.cjs
8. .github/workflows/consult-browser-postgres.yml
9. docs/clinical_docs/VISIT_MILESTONE_M5.md

## 实测与新提交 CI

本地使用隔离 React/ASGI/JWT/一次性 SQLite，不读取生产病例、不开外部 AI 网络：

| 检查 | 本地结果 |
| --- | --- |
| 既有八入口前端回归 | 205/205；基线 197 + M5 新增 8 |
| 更新/更正后端 | 36/36；基线 33 + M5 新增 3 |
| 首次保存后端 | 16/16；基线 10 + M5 新增 6 |
| 手工新建与文书生命周期 | 5/5 + 18/18，原检查保留 |
| 后端合计 | 75/75；基线 66 + 新增 9 |
| M2 两种模板契约 | 通过，未修改模板 |
| Vite 构建及分块门 | 通过；初始 JS 445,775 字节，gzip 134,853 字节，六条路由延后加载 |

首次专项发现预览返回字段遗留变量引用并修正；修复后的针对性检查通过。第一轮完整回归首次保存日志尾部未完整记录，不能仅凭退出 0 认定全部通过，因此单独复核该入口，取得完整 16/16 日志。没有删除或跳过测试。

本地没有可用原生 PostgreSQL，不能用 SQLite 替代浏览器/数据库验收。候选 CI 必须绑定实际 PR HEAD 和本文件列出的基线/范围；保留所有原检查与内容绑定，性能门仍为初始 JS 460,000 / 单块 500,000 字节。

CI 在一次性原生 PostgreSQL + Chromium 中继续原有 113 个检查点，新增六个检查点：两轮原文提交/刷新回读、核对首次保存、丢失追问响应的 GET 核对与旧令牌拒绝、第三轮更正/同步更新，以及两种草稿 DOCX 的原文/来源核验。成功 API 响应不伪造；仅命名故障场景丢弃真实 POST 完成后的响应。

首个候选 `938a2b245bc7677b154d76035785288d2ce5c406` 的五项检查通过，浏览器任务失败：新增测试在没有医生补记时寻找仅有补记才显示的范围下拉框。真实两轮刷新回读、首次保存及丢失追问响应核对已通过，但不能据此声称完整验收成功。第三轮修订按实际界面先填写合成医生补记再明确选择同步，保留原断言；同时恢复追问成功后的历史列表刷新，并拦截旧账号的列表响应。相关八项界面专项及构建重新通过。

CI 运行日志和 artifact `consult-browser-postgres-<实际 HEAD>` 是候选实测证据。此文档在候选提交前编写，不把待运行 CI 写成已经通过；实际 SHA、CI 链接和终局补在同一草稿 PR 描述。

## 预算与待办

授权上限：九文件、三轮修订、两轮完整本地回归、两次候选 CI、一个分支、一个草稿 PR。
截至第二个候选：三轮实现/修订、一轮完整本地回归，另有针对性测试和首次保存日志复核；将单入口复核保守计入第二轮，也未超过两轮上限。第二候选为获批最后一次 CI；若仍有阻塞须报告并停止继续提交。

提交必须含 `[skip render]`；提交前重新读取正式前后端 Auto-Deploy 与 PR previews 均 Off，不修改这些设置。M5 无合并授权；未执行部署、迁移、生产数据操作或 R1 激活。

M2 医生审阅及 Word/WPS 桌面实机分页验收仍待补齐。CI 对 DOCX XML、字段和草稿核对的验证不替代医生审阅或真实 Word/WPS 分页。M2/M3/M4/M5 发布继续暂停。PR #41 与 M5 将来集成时需重新核对重叠 App/CI 文件，并运行合并后的实际内容 CI，不能舍弃任何里程碑测试。

## PR #42 审阅后的五文件补修（2026-09-24）

第二候选 `7b49bf2a881ec5ae56186175cb6167df2c2f1be4` 的六项 CI 已通过，但后续审阅发现未覆盖的长度边界：完整已提交快照被重复序列化进本地草稿 sessionContext，两轮合法长文本使其超过独立的 100,000 字符上限。草稿写入被拒绝并清除旧副本，随后未提交输入不能在刷新后恢复。页面会提示存储失败；服务端已提交原文没有被删除。隔离复现中上下文为 110,139 字符，整份草稿仍低于 250,000 总限制。

用户已补充批准：仅修改原范围内五文件，增加一轮修订、一轮完整本地回归、一次新提交 CI；复用本分支和草稿 PR #42，不合并。补修五文件为 App.jsx、consult-first-save.test.jsx、draft_checks.cjs、consult-browser-postgres.yml 和本记录；累计 PR 仍限定原九文件。

实现复用后端已有的 64 位十六进制 answers_token，与当前问题、模板键共同构成紧凑的草稿上下文。服务器原文继续通过认证 GET 回读；不截断答案、不提高草稿容量、不使用弱摘要判断相同。旧数组格式草稿仍与完整当前答案比较后恢复；旧服务端缺少有效令牌时保持原数组格式及原容量规则。已保存的紧凑草稿若遇到令牌缺失、令牌变化、问题或模板变化，则保留待提交原文到待整理记录，不自动提交给新问题。

实测（补修后的本地候选）：
- 新增八项 React 专项通过：超过 100,000 字符的两轮记录后恢复短输入、旧数组草稿升级、无令牌旧响应、令牌/问题/模板变化、令牌缺失以及账号隔离；刷新恢复不产生额外 POST。
- 一轮完整回归：前端 213/213（原 205 + 新增 8），后端 75/75（36 + 16 + 5 + 18）；未删除、跳过或放宽原测试。M2 模板契约通过。
- Vite 构建与分块门通过：初始 JS 445,845 字节，gzip 134,846 字节，六条延后加载路由；原 460,000 / 500,000 门槛不变。本地依赖缓存的 .bin 包装器路径失效，首次构建未启动；直接调用同一 Vite 包的 bin/vite.js 后构建完成，未更改依赖。
- 静态门在有未提交修改的工作区报告 postcommit worktree dirty；须在干净的最终候选上复核，不修改冻结的治理文件或降低门槛。
- 原生 PostgreSQL + Chromium 新增长记录场景已加入原测试入口：真实界面分两轮填写六个合法长文本，读取实际响应/数据库回读，再填写短输入并刷新恢复，断言原文保留且没有额外写入。此处在新提交 CI 前记录，不能将其写成已经通过。

本轮用量：一轮补修、八项定位专项、一轮完整本地回归；新候选 CI 上限一次。CI 必须重新绑定补修后的内容，额外验证相对 `7b49bf2a...` 仅改获准五文件，继续保留相对 M3 基线的九文件范围和全部原哈希门。最终提交、树、静态门和新 CI 结果登记在同一 PR 描述，旧提交的成功不作为补修后的验收。

提交前再次确认双端 Auto-Deploy 与 PR previews Off，提交含 `[skip render]`；不合并、不部署。M2 人工审阅及 Word/WPS 实机分页验收、所有生产操作边界均继续保留。

## 长页面证据采集补修（2026-09-24）

五文件补修候选 `a877ea851b053b072009ce8182557b66af591014` 实际新 CI 为五项成功、一项失败。前端 213 项、后端 75 项及截图前执行的长记录原文/恢复断言通过，但随后全页截图超时；错误收尾再次尝试全页截图，延迟了退出。原 record 在截图前登记 PASS，不能把该标签或 passed 数组当作完整验收成功。后续草稿损坏/配额和后续浏览器入口未完成，该轮整体失败。

用户新增批准：仅补修 draft_checks.cjs、consult-browser-postgres.yml 和本记录三个既有文件，一轮修订、一次新提交 CI；继续复用草稿 PR #42，不合并。无需重复业务实现或整套本地回归；新 CI 仍运行全部原有检查。

本次仅修复证据流程：
- 长记录场景保留所有既有原文、刷新恢复、无额外写入断言；将十二段完整合成原文、提交/回读答案、页面显示原文、本地草稿、恢复后输入和写入计数保存为 `m5-long-draft-readback.json`，另截图恢复后输入所在的 1440×1000 视口。其他成功场景仍使用原全页截图。
- record 在截图成功之后才追加 passed 并输出 PASS；截图或 JSON 写入失败仍让整轮失败，不忽略失败或扩大超时。
- 失败收尾截图改为视口并限时 5 秒，DOM 文本保留也限时 5 秒；保留最初错误与非零退出状态。
- CI 更新该脚本的精确内容绑定，并新增相对 `a877ea8...` 的三文件范围门；既有相对 `7b49bf2...` 五文件和 M3 基线九文件范围、其他内容绑定及性能门均保留。

必要本地专项通过：注入截图失败后不登记或打印成功；正常截图完成后才登记，原普通场景仍为全页截图；错误收尾截图失败仍保留最初错误、非零退出、JSON/DOM 证据并关闭浏览器。脚本语法、工作流 YAML、34 个内容绑定、全部 16 个原场景名称、长场景原断言和精确三文件范围检查均通过。这些证据流程专项不替代真实 Chromium/PostgreSQL CI。

本记录在新提交前编写。新提交、唯一一次新 CI 的实际结果将登记在同一 PR 描述；不以旧提交成功替代。提交前重新核验正式双端 Auto-Deploy 与 PR previews Off。M2 医生审阅及 Word/WPS 实机分页仍待补齐，发布限制和生产操作边界继续有效。
